{
  "name": "skill-graph-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install --include=dev && npm run build",
    "build:backend": "cd backend && npm install --include=dev && npm install sqlite3 --build-from-source --force && npm run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd backend && npm start"
  }
}
